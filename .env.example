# IdP root (no trailing slash). Prefer SSO_BASE_URL; IDP_URL is a Laravel-package alias.
SSO_BASE_URL=https://sso.example.com
# IDP_URL=https://baaboo-sso.test
# When APP_ENV=local and SSO_BASE_URL/IDP_URL are unset, uses SSO_LOCAL_BASE_URL or https://baaboo-sso.test
# SSO_LOCAL_BASE_URL=https://baaboo-sso.test

# Tool identity — from IdP project OAuth credentials screen
SSO_PROJECT_ID=your-tool-slug
SSO_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
SSO_CLIENT_SECRET=replace-me

# This app's public URL — redirect URI is always {APP_URL}/oauth/callback
APP_URL=https://your-tool.example.com

# Optional
SSO_REDIRECT_AFTER_LOGIN=/
SSO_REDIRECT_TO_IDP_LOGOUT=true
# SSO_COOKIE_SECURE=true
# SSO_JWKS_CACHE_TTL=3600
# SSO_HEARTBEAT_INTERVAL_SECONDS=60
# TLS for calls to the IdP. Preferred local-dev setup: point SSO_CA_BUNDLE at a
# combined CA bundle including your local root CA, so verification stays on.
# EnvKit ships one (Mozilla roots + EnvKit CA):
# SSO_CA_BUNDLE=C:/ProgramData/envkit/certs/cacert.pem
# Without a bundle, verification defaults to false when APP_ENV=local
# (self-signed local certs) and true otherwise. Override explicitly:
# SSO_VERIFY_SSL=false
